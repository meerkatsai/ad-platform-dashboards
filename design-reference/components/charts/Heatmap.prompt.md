Performance matrix: Campaign × Placement, Product × Campaign, Keyword × KPI, Campaign × Device. Sequential ember tint, values always printed.

```jsx
<Heatmap rows={campaigns} cols={['Top of search','Rest of search','Product pages']} values={roas} format={fmtX} />
```