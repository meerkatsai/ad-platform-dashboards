Rankings with many named categories: campaigns, ASINs, keywords, search terms, locations. Neutral bars, selection in ember.

```jsx
<BarsHorizontal items={top10} format={fmtINR} selectedId={sel} onSelect={setSel} />
```