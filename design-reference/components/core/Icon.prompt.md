Lucide glyph (path data inlined, currentColor). Use for every icon; never hand-draw new SVGs — copy path data from lucide.dev into the map.

```jsx
<Icon name="pin" size={16} />
```
Available: chevron-down, download, grip-vertical, calendar, arrow-up-right, arrow-down-right, pin, pin-off, layers, filter, x, search, refresh-cw, external-link.