Text button for dashboard actions; primary is reserved for the single main action on a screen (e.g. Export).

```jsx
<Button variant="primary" icon="download">Export</Button>
<Button size="sm">Show more</Button>
```
Variants: primary (ember), secondary (hairline), ghost. Sizes md 32px, sm 26px.