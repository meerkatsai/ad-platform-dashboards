Exclusive 2–4 way toggle; the standard period switch is D / W / M.

```jsx
<Segmented size="sm" value="D" options={[{value:'D',label:'D'},{value:'W',label:'W'},{value:'M',label:'M'}]} onChange={setPeriod} />
```